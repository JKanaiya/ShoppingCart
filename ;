import { describe, it, expect } from "vitest";
import { render } from "@testing-library/react";
import Shop from "../components/Shop";

describe("Shop", () => {
  it("Renders a list of cards with the desired format", () => {});
});
